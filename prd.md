# LMA：滑坡连续监测智能分析 Agent 产品需求文档

**版本：V2.0**
**产品名称：LMA（Landslide Monitoring Agent）**
**产品定位：面向滑坡连续监测业务的智能调查与分析助手**

------

# 1. 文档目的

本文档定义 LMA 当前及近期迭代的业务目标、功能范围、分析流程、证据规则、交互要求和输出边界。

LMA 不建设新的完整监测平台，而是在已有北斗/GNSS 监测平台之上，通过大语言模型 Agent 自动组织数据查询、趋势分析、图形复核、多站对比、气象关联以及后续调查步骤，帮助监测人员快速理解监测数据中发生了什么，并发现值得进一步核查的变化。

系统核心目标不是替代专业人员完成最终灾害判定，而是降低连续监测数据调查的人工成本，提高：

- 缓慢形变发现能力；
- 异常复核效率；
- 多站点对比效率；
- 数据质量问题识别能力；
- 多源证据组织能力；
- 日常巡检的一致性；
- 分析过程的可追溯性。

------

# 2. 产品定位

LMA 定位为：

> **面向滑坡连续监测场景，以 GNSS/北斗位移数据为核心，结合气象、图表视觉、空间地形和后续地质资料进行多源调查的 LLM Agent。**

系统主要回答：

> 当前监测点发生了什么变化？
> 这种变化是否持续？
> 与过去相比是否异常？
> 与同组其他监测点相比是否具有局部性？
> 当前是否存在加速迹象？
> 数据是否可靠？
> 是否存在需要人工进一步调查的线索？

系统可以基于已有证据进行审慎的滑坡相关分析，例如：

- 某点存在长期持续位移；
- 当前变化更符合缓慢变化而非短时跳变；
- 近期变化速率有所增加或降低；
- 该点变化明显区别于同组监测点；
- 某次降雨与位移变化在时间上接近；
- 当地坡向与位移方向具有一定一致性；
- 当前现象可能与坡体缓慢变形有关。

但这类内容必须明确属于**分析推断**，不得与直接观测事实混写。

------

# 3. 核心业务原则

## 3.1 事实、分析、推断必须分层

所有最终回答必须区分三种信息。

### 观测事实

来自监测平台或确定性计算，可直接验证，例如：

- N/E/U 坐标；
- 累计位移；
- 时间窗口净变化；
- 鲁棒趋势斜率；
- 最大单步变化；
- 缺测区间；
- 降雨量；
- 同组监测点同期变化。

这些信息允许使用明确语气。

例如：

> SCWM-04 在该时间范围内北向净变化约 −18 mm。

### 分析判断

由多个事实组合形成的监测分析结论，例如：

> SCWM-04 在三个方向均表现出长期同向变化。

> 最近六周仍有累积变化，但未发现持续加速。

这类判断必须能够追溯到相应数值证据。

### 专业推断

涉及地质意义、滑坡相关性或诱因关系，例如：

> 该变化可能与坡体缓慢变形有关。

> 位移变化与强降雨在时间上接近，不排除存在一定关联。

必须使用：

“可能”“疑似”“需要进一步核查”“目前证据不足以确认”等表达。

不得把推断写成观测事实。

------

# 4. 系统不采用统一固定形变阈值

不同监测点具有不同的：

- GNSS 噪声水平；
- 安装环境；
- 地质条件；
- 坡度；
- 位移方向；
- 历史稳定范围；
- 数据质量。

因此 LMA 不应依赖统一的：

> 位移超过 X mm 即异常
> 速度超过 X mm/day 即滑坡

作为主要识别方法。

当前系统应采用：

**站点自身历史基线 + 当前窗口变化 + 变化速率 + 趋势持续性 + 同组对比 + 数据质量 + 多源证据**

共同完成判断。

绝对阈值可以作为工程保护参数或特定项目明确提供的业务规则存在，但不得被描述为普适滑坡判据。

------

# 5. GNSS 位移基准

系统使用监测平台在设备稳定运行后确定的固定初始参考坐标作为统一位移基准。

对于每个数据点计算：

- ΔN：北向相对位移；
- ΔE：东向相对位移；
- ΔU：垂向相对位移；
- H：水平合成位移；
- D：三维合成位移。

其中：

H = √(ΔN² + ΔE²)

D = √(ΔN² + ΔE² + ΔU²)

固定参考坐标用于回答：

> 当前点已经相对初始稳定状态移动了多少？

分析窗口首末差值用于回答：

> 在本次调查时间范围内又变化了多少？

二者必须明确区分。

不得把“相对固定参考坐标的累计偏移”与“当前时间窗口净变化”混为同一指标。

------

# 6. 核心数值特征

系统保持轻量数值分析，不建立复杂黑盒预测模型。

每个分析窗口至少计算：

### 6.1 当前累计偏移

相对于固定初始参考坐标：

- ΔN；
- ΔE；
- ΔU；
- H；
- D。

用于判断监测点当前总体偏离程度。

### 6.2 窗口净变化

当前分析窗口首点至末点：

- N 净变化；
- E 净变化；
- U 净变化；
- H/D 净变化。

用于判断当前窗口是否仍在继续发生变化。

### 6.3 鲁棒趋势

使用抗异常值影响的趋势估计方法，例如 Theil–Sen slope，获得：

- N 趋势；
- E 趋势；
- U 趋势；
- H/D 趋势。

主要用于识别缓慢、持续变化。

### 6.4 最大单步变化

识别：

- 突然跳变；
- 数据解算异常；
- 设备状态变化；
- 短时间真实位移候选。

单步变化不得直接解释为滑坡。

### 6.5 跳变后持续性

判断一次跳变发生后：

- 是否保持在新的水平；
- 是否继续沿同方向变化；
- 是否快速恢复。

用于区分：

持续变化与孤立粗差。

### 6.6 数据完整性

至少识别：

- 缺测比例；
- 连续缺测；
- 采样频率异常；
- 时间间隔异常；
- 大量重复值；
- 数据不足。

------

# 7. 慢速形变识别

缓慢形变是 LMA 的核心业务场景。

系统不得仅依据最近 24 小时或最近几天变化量判断监测点稳定与否。

例如：

一个点已经连续三个月每天移动 0.2 mm，但最近一天仅变化 0.2 mm。

如果只观察 24 小时，该点可能被错误判断为“稳定”。

因此系统必须采用**短窗口 + 长窗口联合分析**。

## 7.1 日常巡检默认分析窗口

监测人员询问：

> 最近情况怎么样？
> 今天是否正常？
> 这个点现在稳定吗？

系统不得只依赖过短窗口。

应自动形成至少两个尺度：

### 近期窗口

重点回答：

> 最近是否发生新的变化或加速？

实际范围由 Agent 根据用户目标、变化速度、异常线索、数据质量和可获得的数据覆盖自主选择。

### 长期背景窗口

重点回答：

> 当前是否位于一个持续累积过程之中？

数据不足时使用能够获得的有效历史，并明确说明覆盖范围。

长周期趋势分析优先采用每日固定时刻数据，减少日内周期波动的影响。

短周期异常复核使用小时级数据。

------

# 8. 同刻趋势分析

长期趋势判断优先使用每日固定时刻进行同期对比。

具体默认起点只在 `backend/app/agent/prompts/system.md` 中维护，并允许 Agent 按实际证据调整。固定时刻没有特殊地质意义，其作用是让每天数据处于相近观测条件，从而降低日周期波动对慢速趋势识别的干扰；必要时可以选择多个固定时刻对比昼夜变化。

固定时刻发现趋势后，应使用完整小时级数据进一步检查：

- 日内周期；
- 突发变化；
- 缺测；
- 台阶；
- 异常发生时间。

------

# 9. 监测图视觉分析

视觉模型是辅助分析通道，不是最终判定器。

系统生成至少以下图形：

- N/E/U 原始时序；
- N/E/U 累计位移；
- 水平/三维合成位移；
- 必要时的局部放大图。

视觉模型重点观察：

- 持续变化；
- 突发变化；
- 未见明显变化；
- 趋势转折；
- 台阶；
- 异常区段；
- 图形整体形态。

视觉模型不得负责输出精确数值。

视觉发现必须回到原始 GNSS 数据进行数值复核。

最终结果统一分为：

### 数值证据支持

视觉发现与原始数据计算结果一致。

### 存在变化但证据不足

确实存在一定变化，但不足以确认其性质或持续性。

### 复核未获数值支持

视觉模型发现了异常，但数值数据未支持，应视为视觉误判或低可信候选。

------

# 10. 调查状态

调查状态用于帮助监测人员安排关注优先级。

它不是地质灾害预警等级。

建议使用以下状态。

### 未见明显变化

当前窗口及长期背景均未发现明显持续变化。

### 持续关注

存在长期趋势、相对同组偏大或其他值得继续观察的现象，但当前不存在充分证据支持明显加速或突发变化。

### 需人工复核

出现以下一种或多种情况：

- 明显持续变化；
- 变化速率明显增加；
- 多方向协同变化；
- 数据与视觉均支持异常；
- 多源证据相互支持；
- 现有证据无法排除重要风险变化。

### 数据不足

例如：

- 长时间缺测；
- 有效样本不足；
- 关键降雨期间数据中断；
- 初始参考坐标缺失；
- 数据质量无法支持判断。

### 暂无法分析

平台、站点类型或数据源当前不支持该分析。

每个状态必须同时给出：

> 为什么得到这个状态。

禁止只输出状态名称。

------

# 11. 气象关联分析

天气数据主要作为环境背景证据。

当 GNSS 出现值得关注的变化时，可以查询：

- 异常前数日降雨；
- 异常期间降雨；
- 累计降雨；
- 强降雨时段；
- 必要时温度、积雪等环境信息。

系统允许表达：

> 位移变化与降雨过程在时间上接近。

> 降雨结束后数日仍观察到变化。

但如果只有时间对应关系，不允许表达：

> 本次滑坡由降雨导致。

气象关联的证据等级应由以下信息共同决定：

- 时间先后；
- 时间距离；
- 位移变化持续性；
- 同组监测点响应；
- 是否存在长期背景趋势；
- 是否存在其他可能解释。

------

# 12. 滑坡相关推断边界

新版 LMA 允许进行滑坡相关推断，但不能越过证据能力。

可以表达：

> 该点存在持续缓慢位移特征，建议持续关注。

> 当前变化可能与坡体缓慢变形有关。

> 当前未观察到持续加速特征。

> 如结合后续地形资料确认位移方向与坡向一致，则滑坡相关性将进一步增强。

不能仅凭 GNSS 数据表达：

> 该点正在顺坡滑动。

除非系统已经获得对应：

- 坡向；
- 坡度；
- 地形；
- 点位所在坡体位置；

等空间证据。

不得表达：

> 已经发生滑坡。

> 即将发生滑坡。

> 将在 X 天内失稳。

> 必须立即撤离。

这些内容不属于当前系统能力。

------

# 13. 地图、地形和地质能力

地图空间分析作为下一阶段核心增强能力。

监测站点具备有效经纬度后，系统应能够在前端地图上展示：

- 监测点位置；
- 监测点分组；
- 基准站；
- 移动站；
- 当前调查状态；
- 当前位移方向；
- 位移矢量。

地图不应仅作为位置展示，而应逐步成为空间证据入口。

前端地图必须始终优先展示当前站点和具备有效坐标的同组站点；测点标记不得依赖地质、断层或底图图层成功加载，也不得在缩放或平移后跨越地图边框。展开地图应脱离消息工具卡的尺寸约束，并提供明确关闭按钮与 Esc 键关闭能力。

## 13.1 地形信息

系统可根据 DEM 获取：

- 海拔；
- 坡度；
- 坡向；
- 地形起伏；
- 沟谷、坡顶等基础地形关系。

获得坡向后，系统才允许进一步比较：

> GNSS 水平位移方向是否与坡向近似一致。

## 13.2 地质信息

后续可接入：

- 区域地质图；
- 地层；
- 岩性；
- 构造；
- 断层；
- 已知地质灾害点；
- 工程地质资料。

地质资料属于增强证据，不要求当前版本全部具备。

## 13.3 空间分析表达

允许：

> SCWM-04 水平位移总体指向东南；当地坡向资料显示主要坡向同样接近东南，两者方向具有一定一致性。

不得在未获取地形信息时表达：

> SCWM-04 沿顺坡方向滑动。

------

# 14. 日常巡检模式

LMA 应适用于监测人员每日例行检查。

当用户询问：

> 今天哪些点需要关注？

系统应按照分组或指定站点自动执行：

1. 获取近期数据；
2. 获取足够的历史背景窗口；
3. 检查数据完整性；
4. 判断近期变化；
5. 判断长期趋势；
6. 与同组监测点比较；
7. 对重点监测点进行图形复核；
8. 必要时查询近期天气；
9. 输出关注优先级。

最终优先回答：

> 哪些点需要看。

而不是首先输出大量技术分析过程。

------

# 15. 多源证据原则

最终分析的可信度应来自证据之间的一致性，而不是来自模型措辞。

证据来源包括：

**一级核心证据**

- GNSS 原始数据；
- 固定参考坐标；
- 数值特征。

**二级比较证据**

- 同站历史；
- 不同时间窗口；
- 同组监测点。

**三级辅助证据**

- 图表视觉；
- 天气；
- 地形；
- 地质；
- 后续现场资料。

Agent 应优先使用高可靠数据解释低可靠数据，而不能反过来。

# 16. 下一步推荐动作

每轮监测分析完成后，系统可以提供 2～3 个最相关的下一步动作。

例如：

- 查看 SCWM-04 最近半年长期趋势；
- 对比 8 月强降雨前后变化；
- 查看同组其他监测点近期情况。

推荐动作：

- 只属于当前最新一轮助手回答；
- 可配置启用或关闭，关闭时不得发起推荐模型调用；
- 启用时在主回答最终完成后生成，确保建议基于完整回答；
- 点击后等同于用户发送该自然语言问题；
- 新一轮问题开始后，旧推荐不再作为当前推荐展示；
- 推荐生成完成后应自动进入当前回答的可视区域，切换会话后仍从 Thread 状态恢复；
- 不进入监测事实本身；
- 不应作为新的系统结论。

------

# 17. 会话与流式交互

每个会话对应独立 LangGraph Thread。

系统必须支持：

- 多轮上下文；
- 历史会话；
- 自动生成标题；
- 重命名；
- 删除；
- 长对话上下文压缩；
- 工具调用过程展示；
- 模型思考过程折叠展示；
- 文本流式输出；
- 下一步推荐动作。

## 17.1 并发会话

用户切换会话时：

- 当前正在生成的会话不得中断；
- 流式状态按 thread_id 独立保存；
- 用户切回原会话后应继续看到当前生成进度；
- 不得因为切换会话导致已生成文本丢失或重新开始。

## 17.2 会话列表状态

正在回复的会话应显示 loading 状态。

生成期间：

- 禁止删除该会话；
- 禁止重命名该会话；
- 原菜单操作区域可直接显示加载状态。

生成完成后恢复正常操作。

标题生成期间会话列表只显示占位闪烁，不得短暂展示由 thread id 拼接的技术名称；无状态标题运行产生的无名称临时 Thread 不得进入业务会话列表。生成失败时保留已确认的“新会话”名称并显示错误，不伪装成标题生成成功。用户停止生成后，任何已发出但未返回结果的工具调用必须记录为“已停止”并闭合消息协议，确保同一 Thread 可以继续提问。用户上翻消息时，“回到底部”按钮显示在输入框上方居中，回到底部后隐藏。

首次发送必须在异步创建 Thread 前防重，并在服务端可见 Thread 前确立标题占位态。停止生成采用 LangGraph interrupt 取消语义；只有在服务端确认 run 停止后，才能修复确实未闭合的工具消息，不得与迟到结果并发写入。

## 17.2.1 官方历史摘要（2026-09-15 更新）

直接使用 LangChain SummarizationMiddleware。压缩由 CONTEXT_TOKEN_THRESHOLD 的消息 token 阈值触发，近期上下文由 CONTEXT_KEEP_TOKENS 的 token budget 保留。计数、触发、安全切点、AI/Tool 配对、摘要重试和消息替换全部沿用官方逻辑，无固定回合数或消息数保留下限，无自定义子类、裁剪算法或空摘要修补。

摘要只存在于 checkpoint 的官方消息中（lc_source=summarization），不另存context_summary；UI过滤内部摘要用户气泡，摘要卡直接投影官方消息。保留summary prompt中的站点、明确时间/时区、数值、缺测与候选/事实边界。摘要使用当前LLM配置、独立COMPRESS_THINKING和输出上限，内部模型不混入主消息流或主usage快照。

LMA的用量分项估算只用于展示，不参与压缩触发或保留决策。删除旧比例、消息数、完整回合配置及私有重试替换；不自动迁移历史会话。

## 17.3 上下文窗口用量

后端应采用本次主模型请求返回的 usage metadata 作为总输入、输出和总 token 的事实来源。历史、摘要、系统提示和工具 schema 使用 LangChain 近似计数器与模型字符/token 规则估算分项，估算之和与实际输入之间的差值必须单列。未得到实际 usage 时不得用估算值冒充实际用量，前端保持上一个已确认快照不变。

前端仅在模型窗口配置有效且已取得完整真实 usage 时，在发送按钮左侧展示环状 LMA 标识；点击后展示实际输入/输出、剩余预算、历史/摘要与系统/工具估算、核算差值、输出预留和压缩触发线。旧 checkpoint 或 usage 缺失时不渲染该入口，不显示“未配置”占位，也不伪造百分比。模型窗口配置缺失或非法时服务启动直接报错。

------

# 18. 工具过程展示

前端可以向用户展示 Agent 正在执行的业务步骤，例如：

> 正在查询监测点……

> 正在获取 GNSS 数据……

> 正在对比同期监测点……

> 正在查看监测曲线……

> 正在核验异常区间……

但不应直接向普通业务人员展示：

- JSON；
- LangGraph 内部状态；
- 参数名；
- Python 函数名；
- 工具内部字段；
- 系统提示词、隐藏推理或未由模型接口显式返回的内部状态。

工具界面只读取明确的 artifact 展示数据，不解析模型侧 content，不提供原始 JSON 或内部字段的“技术详情”入口。参数错误、业务拒绝、无数据和执行失败使用官方 ToolMessage.status=error，原因无需展开即可见；已有的图表与地图仍可查看，并明确未完成的复核或资料缺失。content 保留模型判断所需的事实和受控限制，artifact 只承载可发往客户端的业务数据；凭据、内部请求 URL、原始异常、堆栈与供应商原始错误文本仅记录后端日志，不能放入任何消息或 artifact。公开资料来源链接仍需保留。GNSS 空值、空字符串与非有限数值必须显示为缺测且不参与绘图，不得转换为 0。消息时间只使用服务端 `created_at`，缺失时不显示，并统一按 Asia/Shanghai 格式化。站点状态仅展示正常、离线、告警、故障，其他值统一显示“未知”，不得在前端恢复旧数字枚举兼容。

当主模型接口显式返回 `reasoning_content` 时，前端可将其作为“思考过程”按
模型调用顺序折叠展示，并显示该次模型调用耗时。思考内容使用区别于最终正文的
弱化排版；不得把工具原始结果、系统提示词或 LangGraph 内部状态拼接成思考内容。

主 Agent、会话标题、下一步建议、历史摘要压缩和视觉复核必须分别配置思考开关，
不得由辅助任务继承主 Agent 的思考设置。除主 Agent 外，各辅助任务默认关闭思考；
关闭时不向模型接口发送 `enable_thinking` 扩展字段。

用户看到的是业务进度，不是实现细节。

------

# 19. 上游平台边界

LMA 不重新建设监测设备管理平台。

上游监测平台负责：

- 监测分组；
- 监测点；
- 站点坐标；
- 站点类型；
- 固定初始参考坐标；
- GNSS 原始时序；
- 其他已有监测业务数据。

LMA 通过配置连接上游平台。

本项目不实现独立用户管理系统。

如部署环境需要身份认证，应由：

- 上游业务平台；
- 网关；
- 部署系统；

负责。

------

# 20. 数据存储边界

LMA 原则上不复制建设完整历史监测数据库。

GNSS、站点、分组等业务事实从上游平台实时或按需读取。

LMA 自身仅需要保存：

- Agent 会话；
- Thread 状态；
- 上下文摘要；
- 必要的分析缓存；
- 系统配置；
- 调试及评估日志。

原始监测平台仍然是业务事实来源。

------

# 21. 故障与降级要求

任一辅助数据源失败不得导致整个调查无意义地失败。

例如：

天气服务失败：

> GNSS 分析继续执行，同时说明气象资料暂不可用。

视觉模型失败：

> 数值分析继续执行。

地图/DEM 不可用：

> 不进行坡向判断。

某个同组站点无数据：

> 继续分析其他站点。

所有降级必须明确告诉用户缺失了哪一类证据以及这对结论有什么影响。

------

# 22. 站点及分组解析

站点名称支持自然语言和模糊匹配。

如果只有唯一匹配：

> 可以继续。

如果存在多个合理候选：

> 必须要求用户确认。

不得任意选择。

分组请求应获得整个分组的站点集合。

基准站与移动站必须区别处理。

基准站没有普通移动站形变序列时，应说明其业务角色，而不是简单判定为“数据缺失异常”。

------

# 23. 当前核心能力优先级

## P0：当前核心能力

必须稳定完成：

- LangGraph 多轮 Agent；
- 监测点/分组查询；
- GNSS 数据查询；
- 固定时刻长期趋势分析；
- 小时级细节分析；
- 数据完整性检查；
- 固定参考坐标累计位移；
- 图表视觉复核；
- 数值复核；
- 同组对比；
- 天气查询；
- 综合调查回复；
- 会话流式管理；
- 下一步推荐动作。

## P1：近期重点增强

优先增加：

- 慢速形变双窗口分析；
- 站点历史自适应基线；
- 共模异常自动识别；
- 地图站点展示；
- DEM 高程/坡度/坡向；
- 位移矢量地图展示；
- 日常巡检模式；
- 更结构化的调查状态。

## P2：后续研究能力

可进一步研究：

- 地质图与岩性资料；
- 多类传感器联合；
- 现场巡查记录；
- 地下水；
- 裂缝；
- 深部位移；
- InSAR；
- 自动风险模型；
- 滑坡阶段识别；
- 预测模型。

P2 中风险预测类能力必须经过单独实验验证后才能进入正式业务结论。

------

# 24. 明确不属于当前范围

当前版本不负责：

- 用户注册与账号管理；
- 设备管理；
- GNSS 解算；
- 自动控制监测设备；
- 官方地质灾害预警发布；
- 自动生成政府预警等级；
- 自动撤离指令；
- 精确失稳时间预测；
- 未经验证的滑坡发生概率；
- 自动替代地质专家进行最终灾害认定。

------

# 25. 典型业务场景

### 场景一：单点近期状态

用户：

> SCWM-04 最近怎么样？

系统应自动同时检查：

- 近期小时级数据；
- 近期数日变化；
- 更长历史趋势；

避免因为最近一天变化很小而漏掉长期缓慢形变。

------

### 场景二：同组异常比较

用户：

> SCWM-04 为什么变化这么大？

系统可以主动获取同组站点同期数据。

如果只有 SCWM-04 持续变化：

> 强化局部变化证据。

如果所有点同步跳变：

> 优先提示共同系统因素。

------

### 场景三：强降雨调查

用户：

> 这次变化是不是降雨导致的？

系统获取位移和天气数据后回答：

> 是否存在时间关联。

但不得只依据时间重合直接确认因果。

------

### 场景四：日常巡检

用户：

> 看一下今天这个组有没有需要注意的。

系统应输出：

> 重点关注点 → 原因 → 是否加速 → 数据质量 → 建议进一步调查内容。

------

### 场景五：慢速变化

某监测点最近一天变化很小，但最近三个月持续累积。

系统应明确指出：

> 短期变化幅度不大，但长期趋势仍在持续。

不得仅回答：

> 当前稳定。

------

# 26. 产品成功标准

LMA 的成功不以“模型说出了滑坡”衡量。

真正需要提高的是：

1. 是否能够正确找到相关监测数据；
2. 是否能够发现缓慢持续变化；
3. 是否能够区分局部变化与多点共模变化；
4. 是否能够识别数据质量问题；
5. 是否能够减少视觉误判；
6. 是否能够正确组织天气、地形等辅助证据；
7. 是否能够避免无证据的确定性推断；
8. 是否能够让监测人员快速知道“应该先看什么”；
9. 相同数据重复分析时，核心事实是否稳定；
10. 数据缺失和工具失败时是否能够安全降级。

------

# 27. 最终产品原则

LMA 不应成为：

> 一个看到曲线后替监测人员宣布“发生滑坡”的聊天机器人。

而应成为：

> 一个能够主动获取证据、扩展时间窗口、比较历史趋势、检查同组站点、识别数据问题、调用视觉和气象资料，并将事实与推断清晰区分的滑坡监测调查 Agent。

其核心价值是：

**更早发现值得关注的变化、更快完成异常调查，并把最终专业判断需要的证据准备好。**
