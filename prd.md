# 滑坡监测智能体监测分析功能与业务需求规格说明书

## 1. 文档目的

本文档描述滑坡连续监测智能体中“监测分析与异常调查”相关的业务范围、功能要求、业务规则、结果状态、异常处理和输出边界。

本文档用于指导系统复刻、功能重构和后续迭代。

本文档只定义业务行为，不约束具体编程语言、Agent 框架、数据库、中间件、前端框架或部署方式。

------

# 2. 系统定位

系统定位为：

**面向滑坡连续监测业务的辅助调查智能体。**

系统接收监测人员的自然语言请求，从已有监测平台取得其有权访问的监测站点和 GNSS 数据，对指定监测对象和时间范围进行数据质量检查、位移变化分析、异常证据提取，并可进一步结合天气和监测曲线视觉信息形成异常调查结果。

系统的主要价值不是替代现有监测平台，也不是直接进行滑坡预警，而是帮助监测人员完成：

- 调查对象确认；
- GNSS 数据获取；
- 数据质量检查；
- 位移状态分析；
- 异常候选定位；
- 异常证据整理；
- 天气背景补充；
- 图表视觉辅助复核；
- 多站点对比；
- 调查结果汇总；
- 自然语言调查报告生成。

系统输出属于**监测分析与调查辅助信息**。

系统不得把分析结果直接解释为：

- 已发生滑坡；
- 即将发生滑坡；
- 某次降雨导致了滑坡；
- 官方预警等级；
- 撤离指令；
- 应急处置命令。

------

# 3. 核心业务边界

## 3.1 系统负责的业务

系统负责以下四类能力。

### 3.1.1 监测对象解析

根据用户自然语言请求确定：

- 单个监测站点；
- 多个指定监测站点；
- 某一个监测分组下的全部站点。

### 3.1.2 GNSS 监测分析

对监测站点指定时间范围内的 GNSS 数据进行：

- 数据完整性检查；
- 基准检查；
- 位移特征计算；
- 趋势状态分析；
- 异常候选识别；
- 异常时间窗口提取；
- 证据强度和可信度描述。

### 3.1.3 辅助证据调查

在 GNSS 主分析结果基础上，可选择补充：

- 历史降雨背景；
- 降雨与异常时间窗口的时间关系；
- GNSS 监测曲线视觉分析；
- 视觉异常候选与原始 GNSS 数据复核。

### 3.1.4 调查结果解释

将已经得到的确定性分析结果组织为监测人员可以阅读的自然语言报告。

语言模型负责：

- 理解用户调查意图；
- 组织调查结果；
- 解释已有统计与证据；
- 总结局限性。

语言模型不负责重新计算监测结果。

------

# 4. 系统明确不负责的业务

当前版本不承担以下功能。

## 4.1 不承担自动预警

系统不自动生成：

- 蓝色、黄色、橙色、红色等官方预警等级；
- 滑坡预警发布；
- 撤离建议；
- 应急响应指令。

## 4.2 不承担滑坡预测

当前分析回答的是：

> “监测数据在调查时间范围内表现出了什么现象？”

而不是：

> “未来什么时候会发生滑坡？”

滑坡发生概率、失稳时间预测和灾害风险预测均不属于当前功能范围。

## 4.3 不承担地质成因诊断

GNSS 数据、天气数据和视觉结果可以作为调查线索，但系统不得仅凭这些数据确定：

- 滑坡成因；
- 地层结构；
- 岩土体破坏机制；
- 降雨诱发机制；
- 工程施工影响；
- 地下水影响。

如果未来接入地质、工程、地下水等资料，应作为新的证据来源单独扩展。

## 4.4 不替代上游监测平台

系统不重新建设完整的设备管理和监测数据平台。

站点、分组、GNSS 原始数据等业务数据均来自已有监测平台。

## 4.5 不承担大规模离线分析任务

当前分析属于交互式调查。

一次请求只允许分析有限数量的监测站点。

几百或几千个监测站点的批量扫描、跨小时或跨天后台任务、任务暂停、取消、长期任务管理不属于当前版本。

## 4.6 不接受任意用户图片作为监测依据

多模态分析仅允许使用系统根据当前用户有权访问的 GNSS 数据生成的监测图。

当前不提供用户上传任意现场照片、截图或其他图片直接作为异常分析依据的能力。

------

# 5. 业务参与方

## 5.1 监测人员

监测人员通过自然语言提出调查请求，例如：

“分析一下 A01 监测点最近一个月的数据。”

“比较 A01、A02、A03 最近一周有没有明显异常。”

“分析东侧边坡组所有监测点。”

“看看这个异常期间有没有明显降雨。”

监测人员不需要手工组织监测分析步骤。

------

## 5.2 上游监测平台

上游监测平台负责提供：

- 当前用户有权访问的监测分组；
- 监测站点；
- 站点属性；
- 站点初始参考坐标；
- GNSS 时序监测数据。

系统不得绕过上游平台的用户访问范围。

------

## 5.3 天气数据来源

天气数据属于辅助环境证据。

当前主要用于取得监测站点附近指定历史时间范围内的逐小时降雨数据。

天气数据缺失不得导致 GNSS 主分析失败。

------

## 5.4 视觉模型

视觉模型只用于辅助理解系统生成的监测曲线，包括：

- 整体变化趋势；
- 转折位置；
- 可能的异常时间段；
- 近似视觉读数。

视觉结果不能替代原始 GNSS 数据。

------

# 6. 核心业务对象

## 6.1 监测站点

监测站点是最小分析对象。

业务上至少需要识别：

- 站点名称；
- 站点唯一身份；
- 所属分组；
- 站点类型；
- 是否适用于当前 GNSS 日监测分析。

当前异常分析只对系统认定为适用于 **RTK/PJK 日监测异常分析** 的站点执行。

不支持的站点不得强行进入分析。

------

## 6.2 监测分组

监测分组是一组监测站点的业务集合。

用户提出“分析某分组”时，语义必须是：

> 分析当前用户有权访问的该分组内全部站点。

不能只取第一页、前若干个站点或者随机抽样后宣称完成了“全部站点分析”。

------

## 6.3 分析时间范围

所有监测分析必须具有明确时间范围。

系统统一按照 **Asia/Shanghai** 业务时区解释监测时间。

内部分析时间范围采用：

**[开始时间，结束时间)**

即开始时间包含在分析范围内，结束时间本身不包含。

用户如果没有提供分析时间，当前默认分析：

**上一个完整自然月。**

例如当前日期为 9 月，则默认分析 8 月 1 日 00:00 至 9 月 1 日 00:00。

如果用户显式指定时间：

- 开始时间和结束时间原则上必须同时存在；
- 开始时间必须早于结束时间；
- 不能通过自然语言生成非法超大时间范围绕过资源限制。

------

# 7. 支持的分析范围

系统必须支持三种分析范围。

## 7.1 单站分析

用户明确指定一个监测点。

例如：

“分析 A01 最近七天。”

单站请求只能最终对应一个站点。

如果无法确定唯一站点，应要求用户确认，而不能任意选择。

------

## 7.2 多站分析

用户一次显式指定多个站点。

例如：

“分析 A01、A02、A03。”

每个站点独立解析。

其中一个站点不存在、无权访问或者不支持分析时，不得导致其他有效站点全部无法分析。

------

## 7.3 分组分析

用户明确指定一个监测分组。

系统必须获得该分组的完整站点集合。

单次请求只解析一个明确分组。

如果分组不存在或不属于当前用户可访问范围，不执行监测数据分析。

------

# 8. 站点解析业务规则

## 8.1 解析范围

所有站点匹配只能在：

**当前用户实际有权访问的站点集合中进行。**

语言模型识别出的站点名称不能直接成为最终授权范围。

------

## 8.2 匹配优先级

系统可根据以下信息识别站点：

1. 站点唯一标识；
2. 站点名称；
3. 设备编码；
4. 经过规范化后的名称；
5. 唯一、可信的部分名称匹配。

------

## 8.3 唯一匹配原则

只有能够唯一确定目标时才能继续分析。

例如用户输入：

“分析坝体 1 号点。”

但当前用户范围内存在：

- 坝体 1 号点；
- 坝体 1 号点东；
- 坝体 1 号点西；

系统不得自行任选一个。

------

## 8.4 歧义确认

当存在多个合理候选时，应向用户展示当前权限范围内的候选站点进行确认。

确认过程中：

- 不能展示其他用户站点；
- 用户只能选择本次给出的合法候选；
- 恢复分析前必须重新确认候选仍属于当前用户权限范围。

------

## 8.5 多站部分解析

显式多站请求采用逐项解析原则。

例如请求：

A、B、C、D 四个站点。

其中：

- A 成功；
- B 成功；
- C 不存在；
- D 无权访问。

系统应继续分析 A、B，并明确说明 C、D 未执行。

不得因为 C 或 D 失败而取消 A、B。

------

## 8.6 重复站点

用户重复提及同一个站点时，只分析一次。

最终输出顺序应保持稳定，不能因为分析完成先后不同而随机变化。

------

# 9. 分组完整性规则

用户要求分析某个分组时，必须完整取得该分组站点。

当前业务保护上限为：

- 最多遍历 100 页站点数据；
- 最多解析 5000 个站点记录。

这些上限用于避免异常上游响应导致无限拉取。

取得完整站点集合后，再进行：

- 去重；
- 排序；
- 权限检查；
- 站点类型过滤；
- 同步分析范围检查。

如果某分组包含 100 个站点，而当前交互式分析最多允许 10 个，则不能悄悄只分析前 10 个并称为“该组分析完成”。

正确行为是：

**明确告知当前范围超过交互式分析能力，要求用户缩小范围。**

------

# 10. 交互式分析范围限制

当前默认单次监测分析最多执行：

**10 个受支持站点。**

此外，系统按照小时数据规模控制单次分析量。

当前默认单次分析预计总数据量不得超过：

**5000 个站点×小时数据单元。**

例如：

站点数量 × 时间范围对应的预计小时数

超过限制时，应在读取大量 GNSS 数据之前终止。

当前版本不存在真正的大规模后台分析能力，因此不能假装已经把超大任务提交到后台。

------

# 11. 分析指标

允许分析的基础监测指标包括：

- N：北向分量；
- E：东向分量；
- U：垂向分量；
- H：水平位移模；
- D3：三维位移模；
- N 残差；
- E 残差；
- U 残差；
- H 残差；
- D3 残差。

当前默认分析指标为：

- H 残差；
- U 残差。

用户要求的指标必须属于系统支持范围。

不能因为用户或语言模型生成了一个未知指标名称，就临时执行未经定义的监测计算。

------

# 12. 分析类型

当前支持以下分析意图：

- 趋势分析；
- 异常分析；
- 数据质量分析；
- 环境证据分析；
- 多模态辅助分析。

当用户没有特别指定时，默认执行：

- 趋势；
- 异常；
- 数据质量。

天气和多模态属于可选增强能力，不是完成 GNSS 主分析的必要条件。

------

# 13. 单站监测分析流程

每个可分析站点应独立完成以下业务过程：

**确定调查时间**
↓
**获取站点 GNSS 数据**
↓
**时间范围过滤**
↓
**重复数据处理**
↓
**数据质量检查**
↓
**确定监测参考基准**
↓
**计算位移特征**
↓
**识别趋势和异常证据**
↓
**按需取得天气背景**
↓
**按需执行监测图视觉分析**
↓
**对视觉候选回查原始 GNSS**
↓
**形成站点调查结果**

一个站点的结果不能依赖另一个站点是否成功。

------

# 14. 数据质量分析

每个站点至少需要形成以下数据质量事实：

- 理论应有样本数量；
- 实际有效样本数量；
- 重复样本数量；
- 缺失比例；
- 最大连续缺口；
- N/E/U 有效数据比例；
- 数据可信度；
- 数据质量限制说明。

数据不足本身是一种调查结果。

不得将：

“没有足够数据证明存在异常”

解释为：

“监测点稳定”。

------

# 15. 当前数据质量专有规则

当前稳定算法版本采用以下业务参数：

- 最低有效样本数：3；
- 缺失比例达到 40% 后进入低可信状态；
- 缺失比例达到 80% 后视为数据严重不足；
- 连续数据缺口达到 24 小时需要形成质量提示。

上述值是**当前软件分析规则**，不是适用于所有滑坡和地质条件的通用预警标准。

后续如果调整监测算法，应通过算法版本变更，而不是让语言模型动态修改这些规则。

------

# 16. 位移基准

系统分析必须记录本次位移计算采用的参考基准。

参考基准可能为：

1. 监测平台保存的站点初始参考坐标；
2. 当初始参考坐标不可用时采用分析窗口首个有效样本；
3. 无法获得有效参考基准。

必须记录基准的可信状态：

- 正常；
- 低可信；
- 不可用。

使用首个有效样本作为退化基准时，应明确告诉用户：

> 本次分析使用了临时参考基准，无法等同于站点长期固定参考坐标。

------

# 17. 当前异常证据体系

当前系统中的“异常”指：

**监测数据中值得进一步调查的变化证据。**

它不是“滑坡事件已经发生”的同义词。

当前证据体系可以表达：

- 跳变；
- 阶跃变化；
- 持续漂移；
- 噪声增大；
- 多轴同步变化；
- 垂向突出变化；
- 数据缺口；
- 多站共同数据缺口；
- 短时尖峰；
- 参考基准可疑。

每条异常证据至少应具有：

- 所属站点；
- 关联指标；
- 开始时间；
- 结束时间；
- 必要时的峰值时间；
- 变化方向；
- 变化量；
- 证据强度；
- 可信度；
- 触发原因；
- 使用的判据；
- 数据限制。

这些证据属于调查线索，不直接代表地质灾害结论。

------

# 18. 当前默认异常识别规则

为了能够复刻当前版本，现行稳定规则保留以下业务参数。

## 18.1 持续变化

当前持续变化候选要求综合考虑：

- 持续时间；
- 总位移；
- 数据自身噪声水平；
- 变化速率。

当前默认：

- 最短持续 18 小时；
- 总变化至少 50 mm；
- 相对于鲁棒噪声水平达到约 4 倍尺度；
- 变化速率至少约 2 mm/day。

必须同时理解为当前软件判据，而不是通用地质预警阈值。

## 18.2 跳变/阶跃

当前默认：

- 短窗口约 3 小时；
- 跳变量至少 80 mm；
- 相对于数据噪声达到较高显著性；
- 阶跃变化要求跳变后具有持续性；
- 持续性复核窗口约 6 小时；
- 跳变后波动范围过大时不能简单认定为稳定阶跃。

## 18.3 短时尖峰

当前可识别：

- 幅度较大的短时异常；
- 持续时间不超过约 6 小时；
- 随后回到原有变化范围。

这种情况需要与持续位移变化区分，不能直接按照持续异常处理。

## 18.4 噪声变化

系统可以比较前后数据波动程度。

当前波动标准差增长达到约 2.5 倍时，可形成噪声增大证据。

## 18.5 参考坐标异常

如果固定参考坐标与当前数据之间存在明显异常偏移，系统可以形成“参考基准可疑”证据。

当前参考偏移检查量级约为 500 mm。

该证据表示需要调查：

- 参考坐标是否仍适用；
- 设备是否发生过重新安装；
- 是否存在历史累计位移；
- 是否存在坐标或数据异常。

不能直接把参考偏移解释为当前正在发生持续变形。

------

# 19. 站点状态

每个站点最终应能够形成以下调查状态之一：

### stable

当前分析窗口内未发现达到现行调查判据的明显异常证据，且数据质量足以支持判断。

### watch

存在值得关注的变化证据，需要结合后续数据持续观察。

### review_required

存在需要人工进一步调查或复核的重要证据。

### insufficient_data

数据数量或质量不足，无法可靠判断当前监测状态。

### unsupported

该站点不适用于当前分析方法。

这些状态属于**调查状态**，不是预警等级。

------

# 20. 站点分析摘要

单站结果至少应能够回答：

- 分析的是哪个站点；
- 分析了哪个时间范围；
- 实际取得多少有效数据；
- 理论应有多少数据；
- 缺失比例是多少；
- 数据是否低可信；
- 使用的位移基准是否可信；
- 水平位移当前变化情况；
- 北向、东向、垂向变化情况；
- 必要时的三维位移变化；
- 最大变化量；
- 当前趋势状态；
- 是否存在异常候选；
- 发现了多少条主要证据；
- 分析存在哪些限制。

------

# 21. 空数据规则

查询成功但时间范围内没有有效数据时：

状态必须是：

**empty_data**

空数据不是：

- 正常；
- 稳定；
- 无风险；
- 无异常。

正确表达应类似：

> 指定时间范围内没有足够的有效 GNSS 数据，当前无法判断监测状态。

------

# 22. 天气辅助调查

## 22.1 功能定位

天气数据只用于回答：

> “监测异常发生前后，当地是否存在降雨，以及时间上是否接近？”

天气数据不是滑坡因果判断模块。

------

## 22.2 降雨时间窗口

系统可围绕异常证据的：

- 开始时间；
- 峰值时间；
- 结束时间；

统计降雨背景。

当前提供：

- 前后 1 小时累计降雨；
- 3 小时累计降雨；
- 6 小时累计降雨；
- 24 小时累计降雨；
- 72 小时累计降雨。

同时可记录：

- 降雨峰值时间；
- 峰值小时降雨量；
- 异常与降雨峰值之间的时间差。

------

## 22.3 天气匹配状态

天气与监测证据的时间匹配结果分为：

- exact：所需时间范围完整；
- partial：只有部分天气数据；
- unavailable：无法取得有效天气数据。

------

## 22.4 天气结论边界

允许：

> 异常时间窗口前 24 小时存在累计降雨。

允许：

> 位移变化时间与明显降雨在时间上较接近。

不允许：

> 本次位移异常由降雨导致。

更不允许：

> 降雨导致该监测点发生滑坡。

------

# 23. 天气故障降级

如果天气数据：

- 无数据；
- 请求失败；
- 返回内容异常；
- 时间覆盖不足；

GNSS 主分析仍必须继续。

最终报告增加类似：

> 本次天气背景不可用，GNSS 分析结果不受影响。

不得因为天气服务失败而把整个监测分析判定为失败。

------

# 24. 多模态监测图分析

## 24.1 输入来源

视觉分析使用：

**系统根据当前站点已经授权取得的 GNSS 数据生成的监测曲线。**

不得让视觉模型直接访问任意外部文件或用户未授权监测数据。

------

## 24.2 视觉模型可以做什么

视觉模型可以识别：

- 整体趋势；
- 明显转折；
- 曲线形态变化；
- 可疑时间窗口；
- 近似读数；
- 可能遗漏的异常候选。

------

## 24.3 视觉模型不能做什么

视觉模型不能直接：

- 修改 GNSS 数值；
- 把图上近似读数当作传感器精确值；
- 直接确认滑坡；
- 单独决定最终异常；
- 根据图形自行扩大调查时间；
- 根据图形访问其他站点。

------

# 25. 视觉候选复核

视觉模型发现的新异常必须重新回到原始 GNSS 数据进行复核。

视觉候选最终分为三类：

### confirmed

原始 GNSS 数据支持该视觉异常候选。

这里的“confirmed”只表示：

**数据异常候选得到数值证据支持。**

绝不表示：

**确认发生滑坡。**

### suspected

存在一定变化，但当前数值证据不足以确认。

### visual_false_positive

视觉模型认为存在异常，但回查原始数据后没有得到支持。

最终报告必须保持这三者的区别。

尤其：

- suspected 不得写成“已确认异常”；
- visual_false_positive 不得写成“监测数据显示异常”。

------

# 26. 多模态故障降级

视觉模型调用失败时：

- 保留完整 GNSS 数值分析；
- 记录“视觉辅助分析不可用”；
- 不将站点主分析标记为失败。

多模态功能关闭时：

- 不执行视觉分析；
- 不应把“未执行视觉分析”解释为“视觉分析正常”。

------

# 27. 多站点分析

多站点分析不是简单拼接若干单站回答。

系统需要形成统一的跨站点调查结果。

至少包含：

- 用户请求站点数；
- 成功解析站点数；
- 实际支持分析站点数；
- 成功分析站点数；
- 分析失败站点数；
- 跳过站点数；
- 空数据站点数。

------

# 28. 跨站统计

对于成功站点，可以对相同监测指标形成跨站统计。

至少支持：

- 有效站点数量；
- 平均值；
- 最小值；
- 最大值；
- 中位数；
- P95。

可以形成站点排名，例如：

- 水平变化最大站点；
- 垂向变化较大的站点；
- 缺失率较高站点；
- 主要异常候选站点。

所有统计和排名必须由确定性计算得到。

语言模型不得查看几个站点的文字描述后自行“估算平均值”或者“猜测排名”。

------

# 29. 跨站异常分布

系统应能够汇总不同类型的异常证据数量，例如：

- 持续变化；
- 跳变；
- 数据缺口；
- 短时尖峰；
- 其他现行异常证据。

同时需要形成整体数据质量信息，例如：

- 总有效数据量；
- 低可信站点数量；
- 平均缺失比例。

------

# 30. 代表性证据选择

多站点可能产生大量异常证据。

最终报告不需要将所有内部证据全部交给语言模型。

当前默认：

- 每个站点最多选择 2 条主要证据；
- 全组最多选择 5 条主要证据。

选择时应优先保证：

**存在异常的站点尽可能至少保留一条代表性证据。**

在此基础上，再按照证据强度选择全局主要证据。

这样避免一个变化非常明显的站点占满全部证据名额，使其他异常站点完全不出现在报告中。

------

# 31. 整体分析状态

一次分析必须具有明确业务状态。

## 31.1 ok

所有应执行目标均正常完成，并且至少取得一个有效数据结果。

## 31.2 partial_success

存在至少一个有效分析结果，但同时存在：

- 某站点失败；
- 某站点无法解析；
- 某站点无权限；
- 某站点不支持；
- 某个请求目标未执行。

例如 10 个请求站点中：

- 7 个成功；
- 1 个无权限；
- 1 个不支持；
- 1 个上游失败；

最终必须为：

**partial_success**

不能显示为“全部分析成功”。

## 31.3 empty_data

所有能够正常分析的站点均没有有效数据，而且不存在真正的上游分析失败。

## 31.4 failed

没有得到任何可使用的站点分析结果，并且存在实际分析失败。

------

# 32. 其他受控业务状态

除整体聚合状态外，还需要能够表达：

### auth_missing

当前监测平台授权不可用。

### invalid_scope

请求的分析对象不符合要求，例如单站请求指定多个站点。

### station_not_found

没有找到目标站点，或当前用户没有可访问的目标站点。

为了避免泄露权限信息，对外无需区分“站点不存在”和“站点存在但无权限”。

### scope_too_large

站点数量或时间范围超过交互式分析限制。

### unsupported_station_type

当前范围内没有适用于当前 GNSS 分析的站点。

### upstream_error

上游监测数据服务发生业务级故障。

### bad_response

上游返回的数据结构不符合当前业务预期。

------

# 33. 站点级故障隔离

多站分析必须按照：

**一个站点失败，不影响其他站点**

的原则执行。

例如：

A：正常
B：GNSS 查询失败
C：正常
D：空数据

最终仍需要返回：

- A 的完整结果；
- C 的完整结果；
- D 的空数据状态；
- B 的失败说明；
- 整体 partial_success。

不得仅返回“分析失败”。

------

# 34. 不同异常情况的业务处理

| 情况                | 业务处理               |
| ------------------- | ---------------------- |
| GNSS 数据正常       | 正常分析               |
| GNSS 无数据         | empty_data             |
| GNSS 主数据无法取得 | 站点失败               |
| 站点不存在          | 未执行                 |
| 用户无权访问        | 未执行                 |
| 站点类型不支持      | skipped                |
| 天气无法取得        | GNSS 正常，增加限制    |
| 视觉分析失败        | GNSS 正常，增加限制    |
| 天气覆盖不完整      | 标记 partial           |
| 数据字段异常        | bad_response           |
| 用户站点描述歧义    | 请求用户确认           |
| 数据严重不足        | insufficient_data      |
| 分析范围过大        | 拒绝执行并要求缩小范围 |

------

# 35. 授权失效处理

如果分析期间上游监测平台会话失效，系统允许进行一次安全的授权刷新并重新执行本次分析。

不得出现：

- 每个站点分别要求用户重新登录；
- 多个站点同时反复刷新授权；
- 无限重复分析。

刷新后仍然无效时，整体返回：

**auth_missing**

并要求用户重新建立有效的监测平台授权。

------

# 36. 最终调查报告

最终回答不是新的分析算法。

报告只能解释已经生成的可信监测事实。

报告应根据实际情况包含：

- 调查对象；
- 调查时间范围；
- 整体完成状态；
- 数据质量；
- 站点主要位移变化；
- 主要异常证据；
- 多站点比较；
- 天气背景；
- 视觉辅助结果；
- 失败站点；
- 未执行站点；
- 分析限制；
- 必要的后续人工调查建议。

不要求每次使用完全一致的章节模板。

如果多站数据适合比较，可以使用表格；如果不适合，可以使用自然段或列表。

------

# 37. 数值事实规则

所有数值必须来自已经完成的可信计算结果。

语言模型不得自行计算：

- 平均值；
- 极值；
- 分位数；
- 位移；
- 速度；
- 缺失率；
- 趋势；
- 相关性；
- 异常评分；
- 站点排名。

允许为了自然表达进行普通取整，例如：

“50.13 mm”描述为“约 50 mm”。

但不得产生原始结果中不存在的新数字。

------

# 38. 调查结论等级边界

最终文字必须严格区分：

### 数据事实

例如：

> 监测点在该时间段水平残差累计变化约 82 mm。

### 数据异常证据

例如：

> 系统识别到一处持续变化候选。

### 辅助关联信息

例如：

> 异常时间附近存在明显降雨。

### 调查解释

例如：

> 建议结合现场环境和后续监测数据继续复核。

系统不得从前三类信息直接跳跃为：

> 该点已经发生滑坡。

------

# 39. 不确定性披露

存在以下情况时，报告必须主动披露：

- 数据缺失；
- 数据可信度低；
- 基准使用退化方式；
- 天气数据不完整；
- 视觉结果仅为 suspected；
- 部分站点失败；
- 部分目标没有执行；
- 某些站点类型不支持；
- 分析时间范围受到限制。

尤其对于 partial_success，不能将失败信息隐藏在报告之外。

------

# 40. 内部追溯与用户展示分离

系统需要内部追踪：

- 本次分析；
- 站点；
- 数据来源；
- 监测算法版本；
- 异常证据；
- 天气证据；
- 视觉候选及其复核结果。

但内部使用的：

- UUID；
- evidence ID；
- candidate ID；
- 内部字段名；

不得直接出现在面向监测人员的自然语言正文中。

用户应该看到：

> A01 监测点在 8 月 12 日出现持续变化候选。

而不是内部对象编号。

------

# 41. 原始监测数据边界

系统在一次调查过程中可以临时使用完整 GNSS 时序进行计算。

但是监测分析结果本身不应复制保存完整原始时序。

长期保留的分析信息应主要是：

- 查询对象；
- 查询时间；
- 数据来源；
- 数据量；
- 分析摘要；
- 异常证据；
- 数据质量；
- 算法版本；
- 限制信息。

不能因为结果中存在一个“数据引用”就声称原始 GNSS 数据已经被系统长期保存。

------

# 42. 图像数据边界

系统生成的监测图仅作为当前视觉分析过程中的临时输入。

业务记录中可以保留：

- 图表类型；
- 时间范围；
- 包含指标；
- 图像摘要标识；
- 分析结果。

不需要在会话历史和分析结果中长期保存完整图像二进制内容。

------

# 43. 普通查询与监测分析的边界

系统同时保留普通监测问答能力，例如：

- “有哪些站点？”
- “A01 属于哪个分组？”
- “这个站点是什么类型？”
- “今天这里天气怎么样？”

这类请求属于事实查询。

而：

- “分析 A01 最近一个月”
- “判断最近有没有异常”
- “比较这几个站点”
- “调查这个时间段的位移变化”

属于监测分析。

监测分析必须进入固定的调查流程，而不能由语言模型临时决定是否跳过数据质量、异常证据或权限检查步骤。

------

# 44. 分析进度展示

监测分析耗时较普通聊天更长。

用户应能够看到当前所处阶段。

推荐业务阶段为：

**准备分析**
→ **理解请求**
→ **整理调查范围**
→ **确认监测站点**
→ **分析监测数据**
→ **汇总分析结果**
→ **组织调查回复**

进度提示：

- 只显示当前一个阶段；
- 新阶段替换旧阶段；
- 不累积为聊天正文；
- 最终回答开始出现后立即隐藏；
- 不进入历史消息。

进度文字不得展示：

- 内部站点 ID；
- 内部证据 ID；
- 用户凭据；
- 内部错误堆栈。

------

# 45. 会话标题

首次用户消息可以自动生成简短会话标题。

该标题只概括用户第一条消息，不需要针对滑坡监测进行专门改写。

标题生成失败时：

使用清理后的首条用户消息前 20 个字符作为回退标题。

标题失败不得影响监测分析。

------

# 46. 消息复制

用户消息和智能体回答均应允许复制。

复制属于辅助交互功能，不参与监测业务逻辑，也不得改变原始消息内容。

------

# 47. 当前可选能力的默认状态

天气自动证据融合和多模态分析均属于增强能力。

产品默认状态：

- 天气自动融合：关闭；
- 多模态分析：关闭。

关闭这些功能时，GNSS 主分析必须能够独立完成。

显式普通天气查询能力不因自动天气融合关闭而消失。

------

# 48. 当前监测算法版本规则

系统必须记录每次监测分析使用的算法版本和参数版本。

当前稳定默认版本使用现有稳定监测证据算法。

试验性新算法不得自动替换稳定算法。

算法升级必须是显式行为，并保证能够区分：

> 某条分析结果究竟由哪个算法版本产生。

这样不同时间生成的调查结果才能正确比较和复现。

------

# 49. 关键业务原则

整个系统应始终遵守以下领域原则：

### 原则一：权限先于分析

模型理解出的监测对象不等于最终可访问对象。

### 原则二：数据质量先于异常结论

没有足够有效数据时，结论应是“无法可靠判断”，而不是“稳定”。

### 原则三：数值分析先于语言解释

语言模型只解释结果，不创造监测结果。

### 原则四：GNSS 是当前异常调查的主证据

天气和视觉均属于辅助证据。

### 原则五：异常证据不等于滑坡事件

系统识别的是值得调查的监测数据现象。

### 原则六：时间关联不等于因果关系

降雨与位移时间接近不能自动推导因果。

### 原则七：视觉候选必须回到原始数据

视觉模型不能单独确认监测异常。

### 原则八：部分成功必须真实披露

不能为了回答“完整”而隐藏失败站点。

### 原则九：大范围请求不能静默截断

超出当前能力时明确拒绝，而不是分析一部分后冒充全部。

### 原则十：每条重要结论应能够追溯

最终调查结论必须能够找到对应站点、时间、监测数据或辅助证据。

------

# 50. 典型验收场景

## 场景 A：正常单站分析

用户：

“分析 A01 上个月的监测情况。”

预期：

1. 找到唯一 A01；
2. 确认用户有权限；
3. 获取上一个完整自然月数据；
4. 检查数据质量；
5. 计算位移变化；
6. 识别异常证据；
7. 返回站点调查结果。

------

## 场景 B：站点名称歧义

用户：

“分析 1 号点。”

存在多个“1 号点”。

预期：

系统要求用户选择具体站点。

系统不得随机选择。

------

## 场景 C：多站部分不存在

用户请求 A、B、C。

A、B 存在，C 不存在。

预期：

分析 A、B；

C 记录为未执行；

整体状态 partial_success。

------

## 场景 D：分组超过 10 个站点

用户：

“分析整个东侧边坡组。”

分组共有 25 个可分析站点。

预期：

系统确认该组实际包含 25 个站点后发现超过当前同步分析上限。

拒绝本次整组分析并要求用户缩小范围。

不得只分析前 10 个。

------

## 场景 E：部分站点 GNSS 请求失败

A、B、C 三站：

- A 成功；
- B 上游故障；
- C 成功。

预期：

继续完成 A、C；

披露 B 失败；

整体 partial_success。

------

## 场景 F：GNSS 空数据

数据请求成功但没有记录。

预期：

empty_data。

不得输出“监测稳定”。

------

## 场景 G：天气失败

GNSS 分析正常，天气数据无法取得。

预期：

完整输出 GNSS 结果；

天气标记不可用；

说明天气背景没有完成。

------

## 场景 H：视觉发现新候选

视觉模型发现一个明显转折。

原始 GNSS 复核后不支持。

预期：

结果记为 visual_false_positive。

最终报告不得声称该时间存在已确认异常。

------

## 场景 I：视觉候选只有弱证据

视觉模型发现变化，GNSS 存在一定对应变化但不足以确认。

预期：

suspected。

报告使用“疑似”“建议进一步复核”等措辞。

------

## 场景 J：降雨与位移同时出现

异常前 24 小时存在明显累计降雨。

预期：

可以说明时间关联。

不得写：

“降雨导致该点发生滑坡。”

------

## 场景 K：数据严重缺失

指定时间范围大部分 GNSS 数据缺失。

预期：

数据质量进入低可信或数据不足状态。

即使曲线上看起来平稳，也不能直接判定站点稳定。

------

## 场景 L：不支持站点类型

某站点不能执行当前 RTK/PJK 日监测分析。

预期：

不进入异常分析；

记录为 skipped。

如果所有站点均不支持，则整体返回 unsupported_station_type。

------

## 场景 M：10 站中 8 站成功

剩余两个站点一个不支持，一个失败。

预期：

返回 8 个有效站点结果；

同时披露另外两个站点；

整体状态 partial_success；

不得出现“10 个站点均正常分析完成”。

------

## 场景 N：所有站点均无数据

没有任何上游错误，但是所有站点指定时间均为空数据。

预期：

整体 empty_data。

------

## 场景 O：全部分析失败

所有可分析站点均因 GNSS 上游故障无法获得结果。

预期：

整体 failed，并明确说明没有得到有效监测分析结果。

------

# 51. 复刻系统的最小功能闭环

如果重新实现本项目，至少必须完成以下闭环，才能认为“监测分析”功能成立：

**用户提出调查请求**
↓
**识别单站 / 多站 / 分组**
↓
**在用户权限内确定真实目标**
↓
**确定分析时间和指标**
↓
**检查站点类型和分析范围**
↓
**取得 GNSS 数据**
↓
**数据质量和参考基准检查**
↓
**计算确定性监测特征**
↓
**生成异常调查证据**
↓
**可选天气时间关联**
↓
**可选视觉候选 + GNSS 复核**
↓
**单站/多站结果聚合**
↓
**区分成功、部分成功、空数据和失败**
↓
**语言模型基于可信结果生成调查报告**
↓
**明确披露数据质量、失败目标和结论边界**

缺少其中的“权限解析”“数据质量”“确定性数值分析”“异常证据”“部分失败披露”任何一个环节，都不应视为完整的监测异常调查功能。

------

# 52. 最终业务定义

本项目中的“滑坡监测智能体”不应定义成：

> 一个可以调用 GNSS、天气等工具的聊天机器人。

更准确的业务定义是：

> **一个面向滑坡连续监测场景、以已有监测平台数据为基础，通过受控的监测对象解析、GNSS 数据质量与位移分析、异常证据提取、环境和视觉辅助复核以及多站结果聚合，帮助监测人员完成异常数据调查并形成可追溯调查报告的智能辅助系统。**

其中，监测算法负责产生事实和证据，外部数据负责补充调查背景，语言模型负责组织调查过程和解释结果。

系统最终服务于：

**“发现值得调查的监测现象，并把调查所需证据组织清楚。”**

而不是：

**“替监测人员自动宣布滑坡风险结论。”**