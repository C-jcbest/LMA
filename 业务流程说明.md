### 1. 选型

1. 前端使用react
2. 数据库使用postgresql
3. 智能体框架使用langgragh
4. 智能体监测使用langsmith
5. 使用docker运行部署

### 2. 业务流程

1. 用户前端发送消息进行业务询问
2. 后端对用户询问进行处理，进行意图识别，和监测平台相关则进行分析，否则直接回复
3. 进行平台相关处理后可使用平台相关工具进行处理、分析并回复

### 3. 提示词与业务时间

- 主业务策略唯一来源为 `backend/app/agent/prompts/system.md`，视觉策略唯一来源为同目录的 `vision.md`；根目录不再维护第二份完整 Prompt。
- 业务时间统一使用 `Asia/Shanghai`（UTC+08:00）。
- 为最大化供应商 Prompt Cache 命中率，`system.md` 保持完全静态，`create_agent` 直接传入静态系统提示词，不再每回合动态修改或拼装 SystemMessage。
- 当前业务时间与时区唯一由工具 `get_current_time` 获取，模型在需要确认当前时间或解析相对日期时自主调用。
- 主提示词、视觉复核、历史压缩与后续建议统一证据边界：视觉候选不等于确认异常，天气时间关联不等于因果；不推定滑坡阶段，不输出风险百分比、失稳时间或官方预警。基准站在工具层直接说明不适用普通移动站形变分析。
- 天气接口的日期计算与请求时区统一为 Asia/Shanghai。历史范围最多到昨天、单次最多 31 天；当天数据需区分当前/预报与历史资料。最近 24 小时降雨按小时结束时间筛选，缺失任意小时不输出完整总量，并在前端展示覆盖情况及统计窗口。
- 小时降水量代表该时间戳之前一小时的总量，依据 [Open-Meteo 小时参数定义](https://open-meteo.com/en/docs)。预报接口的小时数据不等同于现场雨量计实测。
- 后端回归验证：在 `backend` 目录执行 `.venv/Scripts/python.exe -m unittest discover -s tests -p "test*.py" -v`。真实模型回答和平台数据质量仍需结合运行环境验收。
