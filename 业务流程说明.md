### 1. 选型

1. 前端使用react
2. 数据库使用postgresql
3. 智能体框架使用langgragh
4. 智能体监测使用langsmith
5. 使用docker运行部署

### 2. 业务流程

1. 用户前端发送消息进行业务询问
2. 后端对用户询问进行处理，进行意图识别，和监测平台相关则进行分析，否则直接回复
3. 进行平台相关处理后可使用平台相关工具进行处理、分析并回复

### 3. 提示词与业务时间（2026-09-14）

- 提示词业务依据为根目录 `prompt.md`。主提示词和视觉提示词分别部署于 `backend/app/agent/prompts/system.md`、`vision.md`，内容与文档对应章节保持一致；修改文档后需同步这两个文件。回归测试检查一致性。运行时只读取 backend 内的文件，生产镜像不依赖仓库根目录文档。
- `backend/app/agent/prompting.py` 补充时间、数据可信度与工具能力约束。提示词在模块加载时读取，修改后需要重启后端或由开发服务器重新加载。
- 每个用户回合入口生成带 UTC 偏移的业务时间锚点，同一回合的主分析循环和后续建议共用；下一回合刷新，兼容没有时间锚点的旧会话。临时系统提示词不追加进消息历史。
- 业务时区统一为 Asia/Shanghai。当前时钟显式采用 UTC+08:00，不依赖服务器、容器或浏览器时区；平台 GNSS 时间字符串按业务本地时间解释，不做重复转换。当前时钟实现不用于历史夏令时日期转换。
- 未指定范围的状态判断默认参考最近 7 天小时数据和最近 90 天每日 15:00 数据；“今天”给出当天零点至回合开始时间。用户指定范围、只看指定时段、原始查询或导出按用户要求处理。数据时间范围不足时如实说明。
- 主提示词、视觉复核、历史压缩与后续建议统一证据边界：视觉候选不等于确认异常，天气时间关联不等于因果；不推定滑坡阶段，不输出风险百分比、失稳时间或官方预警。基准站在工具层直接说明不适用普通移动站形变分析。
- 天气接口的日期计算与请求时区统一为 Asia/Shanghai。历史范围最多到昨天、单次最多 31 天；当天数据需区分当前/预报与历史资料。最近 24 小时降雨按小时结束时间筛选，缺失任意小时不输出完整总量，并在前端展示覆盖情况及统计窗口。
- 小时降水量代表该时间戳之前一小时的总量，依据 [Open-Meteo 小时参数定义](https://open-meteo.com/en/docs)。预报接口的小时数据不等同于现场雨量计实测。
- 后端回归验证：在 `backend` 目录执行 `.venv/Scripts/python.exe -m unittest discover -s tests -v`。真实模型回答和平台数据质量仍需结合运行环境验收。

