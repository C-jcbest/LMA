# 本地开发专用依赖（不进入 langgraph build 生产镜像）
# 安装：pip install -r requirements.txt -r requirements-dev.txt

# 本地开发服务器（langgraph dev），生产部署用 Docker 镜像（langgraph build）
langgraph-cli[inmem]==0.4.31

# Windows 上 langgraph dev 的彩色日志依赖
colorama==0.4.6; sys_platform == "win32"
