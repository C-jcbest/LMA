# TODO 清单（2026-09-14 复核：第 1–13 项已完成）

1. [已完成] get_daily_gnss_data 工具支持 sample_times 固定每日取样时刻并优先于采样频率，可用于同刻对比长周期变化；工具会自动调整数据间隔，保证返回数据量不超过 1500 条。

2. [已完成，focus 参数已按后续要求彻底移除] 视觉理解会携带focus参数，导致会主要识别focus中强调的区域而无法进行全局的观察，例如全局窗口持续形变，但因为有一定的缺测或跳变导致focus强调另一子窗口，因此无法识别出持续形变，视觉理解能力应当尽可能灵活，可多次视觉理解+子窗口数值特征提取（参考初始点的累计位移、窗口净变化、鲁棒斜率、最大单步变化、跳后持续性）

   - 追加（2026-09-13）：不再保留 focus 参数降级为"仅供参考"的方案，工具签名、提示词与主智能体提示词中的 focus 已全部移除；子窗口精确判读一律通过更窄时间范围重复调用实现

3. [已完成] 增加下一步推荐动作，点击后自动发送

4. [已完成] 前端流式消息的控制以及正在生成消息的标志需要补充，切换会话后流式输出前端显示可能有问题。增加消息底部的工具栏，目前仅需要复制功能，鼠标悬浮后显示图标

   - 追加（2026-09-13）：修复切换会话后切回时流式内容与已落库的中间 checkpoint 重复渲染的问题（生成中切回：截掉最后一条用户消息之后的服务端历史，交给流式缓冲统一渲染；并处理加载与落地竞态的重拉）
   - 追加（2026-09-13）：停止生成不再仅中断前端——前端捕获 run_id（onRunCreated），停止时调用服务端 cancel API（action=interrupt）真正终止 run；删除会话时同样先取消服务端 run

5. [已完成] 前端会话列表增加正在回复的loading图标，正在回复中无法重命名和删除替换为loading图标。当前回复中切换会话后回到原会话无法正常流式输出

6. [已完成] 视觉识别不要分为两个提示词，仅提供一个工具一个提示词即可，可以重复调用视觉识别工具来精确子窗口，可以进行滑坡推断，说明滑坡可能原因以及整个滑坡过程，但不要以确定的语气，而是提供建议的方式，包括最终的回复也是可以给出滑坡可能原因以及整个滑坡过程、建议和风险提示等内容

7. [已完成] 获取监测数据工具也可以灵活一些，同一时间点的位移对比能更好的判断整体趋势，不同时间点的数据则更好看出周期变化，调整数据获取，可反复调用工具进行数据比较来验证异常

## 实施总原则

- 智能体依据用户目标和实际证据动态决定工具、时间窗、粒度、复核次数与回答结构；以下方案是能力与验收边界，不是固定执行模板。
- 保持 Thread/Checkpoint 为会话历史唯一事实来源；模型输入、工具结果和前端展示状态各自职责单一。
- 先做可独立回归的小改动，再做流式与地图两项高风险重构。建议顺序：11、12 → 9、10 → 8 → 13。
- 每项均需覆盖正常、失败、缺测/空值和恢复场景；未完成代码、测试与文档同步前不得标记完成。

8. [已完成] **重构前端 LangGraph Streaming**

   方案：

   - 引入与当前 SDK 兼容的 `@langchain/react`，以 `useStream` 绑定 `threadId`；先建立薄适配层兼容现有消息卡片，再移除 `streamStates`、`runIdsRef`、`abortControllersRef`、`truncateAfterLastUser` 和手工 checkpoint/stream 去重。
   - 切换会话调用 `disconnect()`（等价于不取消 Run 的断开），目标会话按 `threadId` 重新挂载并 rejoin；Stop 使用 `stop()` 或服务端 cancel，明确取消 Run。
   - 正文、工具调用和推荐动作都从服务端消息/状态派生；前端只保留选中项、展开态等 UI 状态。侧栏运行态从服务端线程/Run 状态获取，不以本地缓冲是否存在作为事实。
   - 采用渐进迁移：先建立集成测试覆盖现有行为，再替换单会话流，最后删除旧状态机；不一次性重写消息卡片。

   验收：

   - 生成中切换会话不会取消后台 Run；切回或刷新页面后可恢复工具卡、已生成正文和后续 token，且无重复消息。
   - Stop 后服务端 Run 确实结束；多个 Thread 并行时状态互不串线。
   - 断网重连超时后回退拉取 Thread 历史，最终界面与 checkpoint 一致。

9. [已完成] **为工具执行引入 LangGraph RetryPolicy**

   方案：

   - 先调整 `ToolNode(handle_tool_errors=True)` 吞异常的现状：瞬时网络异常必须向节点层冒泡，参数/权限/业务错误转换为可读的 ToolMessage，不进入重试。
   - 在 `tools` 节点配置 `RetryPolicy(max_attempts=3, initial_interval=0.5, backoff_factor=2, jitter=True)`，`retry_on` 仅接受超时、连接错误和明确的 HTTP 5xx/429；北斗业务码、4xx、校验错误和空数据不重试。
   - 视觉模型的 JSON/图像降级重试继续保留为“语义修复”，与节点级网络重试分开计数，避免重试放大。

   验收：模拟瞬时失败后可恢复；不可重试错误只执行一次；达到上限后智能体收到可解释错误；日志含工具名、尝试次数和错误类别但不含凭据。

10. [已完成] **完善上下文预算计算**

   方案：

   - 建立统一 `ContextBudget`，预算包含动态系统提示词、历史摘要、消息正文、工具调用/结果、工具 schema 以及输出 token 预留；不再只统计消息和摘要。
   - 优先使用与当前模型匹配的 tokenizer/模型计数接口；未知兼容模型采用可配置保守估算，并用实际 usage 元数据持续校准。配置必须声明上下文上限，缺失时给出可观察告警而非假装精确。
   - 压缩仍按完整用户回合淘汰，但根据剩余预算动态决定压缩量；保留当前回合、关键标识、绝对时间、原始数值、证据不确定性和未解决事项。
   - 摘要只提供事实记忆，不规定下一轮工具路径或回答章节，以免压缩结果僵化智能体。

   验收：边界值测试覆盖中英文、图片/多段内容、tool_calls、长工具结果和动态提示词；压缩后输入加输出预留不超过模型上限；压缩失败不丢历史并可观测。

11. [已完成] **补充监测点空间字段**

   方案：

   - `_station_to_dict` 返回 `latitude`、`longitude`、`altitude`、`coordinate_system`；有效数值转为 number，无效或空字符串返回 `null`，不伪造 `0`。
   - 根据上游接口文档，经纬度坐标系固定标记为 `WGS84`；海拔保留米单位。`StationN0/E0/U0` 是监测初始坐标，不能与地图经纬度混用。
   - 同步工具说明、类型定义、接口参考和前端消费代码，保持旧字段兼容。

   验收：覆盖有效、空值、非法数值和候选重名站点；列表与单站解析返回一致的空间字段。

12. [已完成] **优化会话标题生成**

   方案：

   - 保留自然语言生成，不使用“站点名 + 固定动作”的标题模板；提示模型优先保留可区分的站点名/编号和用户主要意图。
   - 移除当前 `clean_title[:20]` 的破坏性截断。持久化较完整的短标题，由前端 CSS 省略显示；服务端仅做单行、控制字符、包裹符和合理总长度校验。
   - 对超长或无效输出采用词/语义边界压缩；若首条消息含站点标识，回归检查生成标题没有截断该标识。失败仍回退“新会话”。

   验收：覆盖中文站名、带连字符编号、英文、多站点、长问题和提示注入文本；标题自然、可识别、无前缀解释，完整站点标识被保留。

13. [已完成] **将地图作为工具结果嵌入聊天消息**

   方案：

   - 先定义版本化 `site_environment` artifact：中心站、同组站点、WGS84 坐标、海拔、可选地形统计、图层 GeoJSON、数据源/时间/坐标系和缺失原因。模型正文不直接拼接地图配置或远程 URL。
   - 前端增加惰性加载的内联地图卡片与展开面板，不新增一级业务页面。底图只提供卫星影像和地形；叠加中心 Marker、同组 Marker、地质单元与主要断层。
   - 使用可替换地图适配层（优先 MapLibre GL JS）；瓦片、DEM、地质和断层服务地址由受信配置提供，校验域名并展示 attribution，不接受模型生成 URL。
   - 分两阶段交付：第一阶段用第 11 项字段完成站点/同组 Marker、图层切换、缺失态；第二阶段接入经过确认的 DEM、地质和断层数据源，再计算坡度、坡向、500m 高差与最近断层距离。数据源未配置时隐藏对应事实，不让模型补写。
   - 空坐标、单站、部分图层失败、移动端和键盘操作均有降级；地图内容作为辅助空间证据，不替代 GNSS 数值判断。

   验收：地图只在有效 artifact 出现；坐标位置正确、图层可独立开关、来源可追溯；外部图层失败不影响正文和其他工具卡；无 XSS/任意 URL 加载风险。

14. [已完成] 建立端到端测试集验证各种情况下回复是否正常，是否对用户友好

   - 新增前端会话关键路径集成回归，覆盖完整工具结果、取消中的未闭合工具调用、推荐动作点击、回到底部按钮和取消态文案；与后端 15 项单元测试、前端生产构建共同作为离线回归基线。
   - 真实模型措辞、上游数据质量和外部瓦片可用性仍属于部署环境验收，不以模拟数据冒充通过。

15. [已完成] 当前下一步建议消失了，检查问题；将回到底部放在消息输入框上方中间展示，已经在底部时隐藏；标题生成时会短暂出现监测点-01a93e类似标识而后展示总结的标题，定位问题并修复，正常效果是占位闪烁并展示生成的标题，失败则展示默认新会话标题名称；取消回复后工具调用一直显示正在进行标志，并且再次请求后显示⚠️ 会话请求失败：An internal error occurred，修复；生成消息时标题不需要前后都展示loading，仅保留后面的；

   - 推荐动作到达后触发底部跟随；回到底部按钮移至输入框上方居中，并按实际滚动距离显隐。
   - 标题生成期间轮询保留本地占位态，不再回退显示 thread id 派生名称；失败统一落为“新会话”。侧栏运行态只保留标题右侧 loading。
   - Stop 后为未完成 tool_call 补齐结构化取消 ToolMessage，取消态不再旋转，并在 checkpoint 清理完成前锁住输入，保证下一轮消息协议合法。

16. [已完成] 地图交互有问题，比如放大、缩小等会出现无法关闭的情况，而且出现的位置有问题，只有关闭工具折叠栏才能正常关闭；没有出现marker，请在地图上展示一些方便监测人员查看的信息，并适当调整地图初始放大倍率，当前地图查看过小；不需要在回复中标注地理信息来源，仅需要将一些需要注意的信息标注；

   - 测点改为不依赖外部图层 load 事件的 DOM Marker；当前站点使用金色，同组站点使用浅色，并显示名称、设备状态和可点击详情。
   - 单站默认缩放提高至 15，多站自适应范围最大缩放提高至 16；展开态改为独立全屏浮层，可由右上角按钮、遮罩或 Esc 关闭，不受工具折叠容器影响。
   - 地图内突出当前关注站点、有效同组测点数和设备状态；详情区仅保留资料限制，地图供应方 attribution 仍按许可要求由底图库展示。

17. [已完成] 地图放大后再缩小会导致marker标记能够位于边框上方；还是没有看见下一步的可点击项；在会话第一次消息发送时仍然出现临时会话，类似监测点-01a93e，且这次观察到出现了两个检查并修复；取消回复后的工具处理是否符合官方规范？会不会有其他问题？

   - Marker 在每次缩放、平移和容器尺寸变化后按投影点与实际 DOM 尺寸做边缘裁决，边缘标记隐藏，地图容器同时隔离裁切。
   - 下一步建议由模型结合本轮问题与回答判断是否适用，仅接受约定的 JSON 数组；格式异常或独立调用失败时显示错误，不通过正则或固定模板猜测业务建议；新回合开始清除旧项。
   - 首次发送在任何异步操作前上锁，客户端预生成 thread id 并先登记标题骨架态；会话列表只投影带明确名称的业务 Thread，过滤 `session-title` 无状态运行产生的无名称临时 Thread。
   - 取消使用官方 interrupt 语义，并以 `cancel(wait=true, action="interrupt")` 等待服务端收敛后，才对 checkpoint 中确实未闭合的 tool_call 补入取消 ToolMessage，避免与迟到工具结果竞态。

18. [已完成] 上下文计算是否合理，langgragh中应当能够方便的计算token消耗，首先查阅官方文档，然后进行优化改进，尽可能规范功能靠拢官方实现。并且前端进行上下文窗口token的展示，在消息框发送按钮左侧展示logo进度条，点击后能查看更详细的内容

   - 每次主模型请求完成后使用供应商返回的 `usage_metadata` 记录实际输入、输出和总 token；系统提示、工具 schema、历史消息与摘要采用 LangChain `count_tokens_approximately` 和当前模型字符/token 规则估算分项，并单独展示估算与实际输入的核算差值。
   - `context_usage` 随 LangGraph Thread checkpoint 持久化。前端仅在拿到完整的真实 usage 和有效窗口配置后展示 LMA 山形环状进度，点击查看实际输入/输出、剩余预算、估算分项与压缩触发线；旧 checkpoint 或缺失 usage 时保持不显示。
   - `CONTEXT_MODEL_CONTEXT` 使用当前 DeepSeek V4 Flash 官方窗口 `1048576`，且必须为正整数；缺失或非法时服务配置直接报错，不渲染“未配置”占位或伪造百分比。

19. [已完成] 增加主模型思考功能，并在前端折叠展示思考过程与用时

   - 主模型通过 `LLM_THINKING` 控制兼容接口的 `enable_thinking` 参数；关闭时不发送该供应商扩展字段，不支持时由接口明确报错。
   - 后端保留完整响应和流式分片中的 `reasoning_content`，按模型调用记录用时，并随 AIMessage checkpoint 持久化；不从提示词、工具结果或 LangGraph 内部状态拼造思考内容。
   - 前端将思考作为独立消息片段，保持“思考—工具—思考—正文”的实际顺序；默认折叠，标题显示思考状态与用时，展开后使用区别于最终正文的批注式排版。
   - 回归覆盖思考字段透传、流式分片、耗时记录、消息投影顺序、折叠交互与生产构建。
   - 主 Agent、标题、下一步建议、历史压缩和视觉复核使用互相独立的思考开关；四类辅助任务默认关闭，关闭时不发送供应商扩展参数。
   - 增加 `RECOMMEND_ENABLED` 总开关（默认开启）；关闭后清空当前建议且不调用建议模型。开启时仅在主回答最终完成后生成，不依据流式正文前段提前生成。

20. 统一北斗站点经纬度规范化，在数据入口一次性修正，删除天气、地图等模块各自的坐标交换兜底逻辑。

21. 重构 `BeidouClient` 生命周期，复用 HTTP Client 与 SessionUUID，避免每次工具调用重新登录，并为后续不同用户独立 Session 做准备。

22. 将 Stop 后未闭合 ToolCall 的修复逻辑移到后端，前端只负责 `stop/cancel`，删除 `asNode: "tools"` 等对 LangGraph 内部节点的硬编码，参考官方文档这部分该如何实现，前后端如何联通统一

23. 收敛模型 reasoning 兼容层，由后端统一适配 `reasoning_content/thinking` 等供应商差异，前端只消费统一结构，减少 `api.ts` 兼容分支，如果deepseek和其他兼容openai的模型有区别则进行区分，识别到deepseek使用专门的模型依赖，其余的使用openai兼容依赖

24. [已完成] 清理死数据和遗留代码：删除 `admin/监测中心用户`、固定端口提示、“北京今日天气”等不匹配能力的快捷问题；确认删除未使用的 `ToolCallCard.tsx`，将 `_debug_vision.py`、`_dbg_context.py` 中可重复验证的断言迁入正式测试后删除脚本。

25. 拆分过大的 `InlineToolCall.tsx`，按 GNSS、天气、视觉、站点、场地环境分别建立独立 Renderer，顶层仅处理工具状态与分发。

26. 视觉分析里已经出现“固定阈值僵化 Agent”的问题，移除或弱化固定业务阈值，如 `2mm/20%` 跳后持续性判断，工具只返回净变化、鲁棒斜率、最大单步变化、尾部偏移等客观特征，后续结合监测点自身历史噪声判断。`sample_times` 最多 6 个也属于不必要的固定限制，没太大必要再人为限制

27. [已完成] 分析窗口与同刻采样默认值仅在 `system.md` 中维护为可调整的默认起点；`prompting.py` 不再重复生成业务窗口，并要求根据数据覆盖、变化速度、异常线索和用户目标自主扩大或缩小。

28. [已完成] 检查并减少 Prompt、Tool docstring、时间上下文中重复的业务策略，避免多处配置漂移和过度限制 Agent 自主选择时间窗、粒度及复核次数。

29. 优化 GNSS/视觉数据传输，避免大量原始点、`chart_points` 和 PNG Base64 长期堆积在 Thread/Checkpoint；模型侧优先传摘要和数值特征，完整数据仅供前端展示。

30. 弱化下一步推荐动作的关键路径影响，推荐动作不应该成为主回答的“结束条件”，不应该拖慢主要业务回答，推荐生成失败无需作为用户可见业务错误，也不应影响主回答完成。并且下一步建议应当知道工具信息，不能给出和智能体业务无关的内容，调整获取下一步建议能够获得的上下文，并且近几轮的问答和工具信息等内容也应该给到模型

31. 会话列表增加分页或增量加载，减少固定每 3 秒全量轮询。并且应该按照最新回复的会话进行排序

32. 当前地图坡度计算可用，但不要把它演化成判据

    现在是通过中心附近 ±90m 高程差计算坡度、坡向，并用 500m 网格算高差。

    作为：

    > “场地环境辅助背景”

    没问题。

    但不要以后再写：

    ```
    坡度 > 30°
    → 风险高
    ```

    这种固定规则。

    最好输出：

    ```
    DEM source
    resolution
    sampling method
    slope
    aspect
    local relief
    ```

    然后让它只作为调查证据。

33. 修正 `.env.example` 的模型与上下文窗口配置关系，避免 GPT 示例却默认使用 DeepSeek 百万上下文参数；地图、地质、瓦片服务地址改为受信配置而非代码硬编码。

34. 补正式测试：重点覆盖坐标规范化、Session 复用、Stop/Cancel、上下文压缩、不同监测点噪声、自适应异常特征，以及真实 LangGraph Server 流式 E2E。

## 参考

- LangChain 官方 Join & rejoin streams：<https://docs.langchain.com/oss/python/langchain/frontend/join-rejoin>
- LangGraph 官方 Fault tolerance / RetryPolicy：<https://docs.langchain.com/oss/python/langgraph/fault-tolerance>
- LangSmith 官方 Cancel runs：<https://docs.langchain.com/langsmith/cancel-run>
- LangGraph 官方 Manage short-term memory / token counting：<https://docs.langchain.com/oss/python/langgraph/add-memory>
- 北斗平台站点字段与 WGS84 约定：`docs/reference/beidou-platform-api.md`
