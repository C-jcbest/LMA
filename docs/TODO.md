# TODO 清单（2026-09-13 复核：全部完成）

1. [已完成] get_daily_gnss_data工具应当能够使用sample_times参数: 固定每日取样时刻，如 ["03:00", "15:00"]，优先于采样频率，比如分析长段时间的数据，那么每小时的数据在图表上会表现出每日周期性的变化，而隐藏在长周期的持续性形变则不容易被发现，当比较每日固定时间的数据时能够更清晰的进行分辨（通常每日一个点时采取15点的数据）。然后应该能够自己调整数据间隔保证数据量不超过1500条
2. [已完成，focus 参数已按后续要求彻底移除] 视觉理解会携带focus参数，导致会主要识别focus中强调的区域而无法进行全局的观察，例如全局窗口持续形变，但因为有一定的缺测或跳变导致focus强调另一子窗口，因此无法识别出持续形变，视觉理解能力应当尽可能灵活，可多次视觉理解+子窗口数值特征提取（参考初始点的累计位移、窗口净变化、鲁棒斜率、最大单步变化、跳后持续性）
   - 追加（2026-09-13）：不再保留 focus 参数降级为"仅供参考"的方案，工具签名、提示词与主智能体提示词中的 focus 已全部移除；子窗口精确判读一律通过更窄时间范围重复调用实现
3. [已完成] 增加下一步推荐动作，点击后自动发送
4. [已完成] 前端流式消息的控制以及正在生成消息的标志需要补充，切换会话后流式输出前端显示可能有问题。增加消息底部的工具栏，目前仅需要复制功能，鼠标悬浮后显示图标
   - 追加（2026-09-13）：修复切换会话后切回时流式内容与已落库的中间 checkpoint 重复渲染的问题（生成中切回：截掉最后一条用户消息之后的服务端历史，交给流式缓冲统一渲染；并处理加载与落地竞态的重拉）
   - 追加（2026-09-13）：停止生成不再仅中断前端——前端捕获 run_id（onRunCreated），停止时调用服务端 cancel API（action=interrupt）真正终止 run；删除会话时同样先取消服务端 run
5. [已完成] 前端会话列表增加正在回复的loading图标，正在回复中无法重命名和删除替换为loading图标。当前回复中切换会话后回到原会话无法正常流式输出
6. [已完成] 视觉识别不要分为两个提示词，仅提供一个工具一个提示词即可，可以重复调用视觉识别工具来精确子窗口，可以进行滑坡推断，说明滑坡可能原因以及整个滑坡过程，但不要以确定的语气，而是提供建议的方式，包括最终的回复也是可以给出滑坡可能原因以及整个滑坡过程、建议和风险提示等内容
7. [已完成] 获取监测数据工具也可以灵活一些，同一时间点的位移对比能更好的判断整体趋势，不同时间点的数据则更好看出周期变化，调整数据获取，可反复调用工具进行数据比较来验证异常
